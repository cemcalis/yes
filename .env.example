# Example environment variables for deployment

# If you want to use Postgres on the VPS, set DATABASE_URL
# e.g. DATABASE_URL=postgres://user:pass@localhost:5432/aura
DATABASE_URL=

# JWT secret for signing tokens
JWT_SECRET=

# Admin dev login (optional, only for local/dev fallback)
ADMIN_DEV_EMAIL=admin@example.com
ADMIN_DEV_PASSWORD=changeme

# Optional SMTP for sending emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# App settings
NODE_ENV=production
PORT=3000
# Copy to .env and fill values
# Example: cp .env.example .env && edit .env

# Database (prefer managed Postgres). Example format:
# DATABASE_URL=postgres://user:pass@host:5432/dbname
DATABASE_URL=

# App secrets
JWT_SECRET=change_this_to_a_strong_secret

# Optional (if using separate pool url)
POSTGRES_URL=

# Backend port (internal)
BACKEND_PORT=5000

# Frontend settings
# Lokal geliştirme için backend URL (Next.js rewrites buraya proxyler)
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# Optional: enable debug
NODE_ENV=production
# Frontend Environment Variables
# Bu dosyayı .env.local olarak kopyalayın ve değerleri doldurun

# API URL (Production'da Netlify Functions kullanılacak)
NEXT_PUBLIC_API_URL=/.netlify/functions

# JWT Secret (Production'da güçlü bir secret kullanın)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# Node Version
NODE_VERSION=22.20.0
